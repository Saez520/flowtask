---
name: plan-template
description: Standard structure for implementation plans. Load before generating a plan to ensure consistent format and all required sections.
license: MIT
compatibility: opencode
metadata:
  category: planning
  scope: flowtask
---

# Plan Template

Standard structure for all implementation plans generated by FlowTask.
The constructor executes this plan sequentially. Every section must be consumable without ambiguity.

---

## Structure

```markdown
# Plan: CA-{ID} — {Title}

**CA:** {ID}
**Requisito:** [descripción literal del CA]
**Tipo de intención:** [del CA]
**Complejidad:** [del CA]

## TL;DR

> **Summary**: 1-2 sentences
> **Deliverables**: list of files/components
> **Effort**: Quick | Short | Medium | Large | XL
> **Critical Path**: Task X → Y → Z

---

## Decisiones de diseño (CONFIRMADAS)

All technical decisions resolved. ZERO decisions left for the constructor.

- [decision]: [chosen option] — rationale: [why]
- [PENDING: question] — only if requires user input

---

## Scope

**INCLUDES:**
- [what is in the plan]

**EXCLUDES:**
- [what is NOT in the plan]

---

## Capas afectadas

- [ ] types/models
- [ ] data
- [ ] business
- [ ] api/endpoints
- [ ] tasks/workers
- [ ] config

---

## Convenciones a aplicar

Resolved by the planner from project conventions in Engram.
The constructor follows these — does not re-search.

- Naming: [pattern to follow]
- Injection: [constructor / field / annotation]
- Transactions: [if applicable]
- Error handling: [pattern]
- Logging: [prefix/convention]

---

## Archivos a LEER primero

Files the constructor must open for context before creating new ones.
Only list files verified to exist via Engram or scan.

- `path/to/file.ext` — [why it matters]

---

## Artefactos a crear o modificar

Execute in this order (respecting dependencies):

| # | Op | Description | Path | Depends on |
|---|----|-------------|------|------------|
| 1 | CREATE/MODIFY | [complete description] | [path] | [task #] |

**Ordering rules:**
- Model → DTO → Repository → Service → API
- Config before code that consumes it
- Constants/enums before classes that use them
- Tests after implementation (or RED-GREEN-REFACTOR if applicable)
- Implementation + test = ONE task, never separate

---

## Propiedades y configuración

If applicable:

| File | Key | Value | Description |
|------|-----|-------|-------------|
| [file] | [key] | [value] | [what it configures] |

---

## TODOs

- [ ] N. {Task Title}

  **What to do**: clear, actionable implementation steps
  **Must NOT do**: specific exclusions

  **References** (be exhaustive):
  - Pattern: `src/path:lines` — what to follow and why
  - API/Type: `src/types/x.ts:TypeName` — contract to implement

  **Acceptance Criteria** (executable, not inspectable):
  - [ ] verifiable condition with command

  **QA Scenarios** (MANDATORY):
  ```
  Scenario: Happy path
    Tool: [bash / curl / playwright]
    Steps: exact actions with specific data/commands
    Expected: concrete, binary pass/fail

  Scenario: Failure/edge case
    Tool: [same or different]
    Steps: trigger error condition
    Expected: graceful failure with correct error handling
  ```
```

---

## Key Rules

- **Decision complete**: ZERO decisions left for the constructor.
- **Implementation + Test = ONE task**: Never separate.
- **Every task MUST have**: References, Acceptance Criteria, QA Scenarios.
- **QA Scenarios**: Specific tool + concrete steps + exact assertions. Both happy-path AND failure.
- **Only verified files**: Do not reference files that have not been found via Engram or scan.
- **Conventions resolved**: The planner resolves all conventions. The constructor follows them, does not re-search.
